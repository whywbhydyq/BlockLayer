export function ExplanationBlock() {
  return (
    <section className="content-card explanation-block">
      <h2>Builder mode notes</h2>
      <p>Use layer navigation for spheres and domes, row highlighting for the current layer, and copy the current row when you want a small step-by-step instruction instead of a full table.</p>
      <p>Odd diameters have a single center block. Even diameters place the center between blocks, so the highlighted axes and bounds should be followed instead of guessing from a screenshot.</p>
    </section>
  );
}
